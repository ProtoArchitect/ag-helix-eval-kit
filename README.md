# Ag-Helix Evaluation Kit
This repository contains pre-compiled black-box binaries and stress-test scripts for evaluating the SWAR Layer 1 performance of the QTDU format.

## Usage
Run `./run_stress.sh` to execute the multi-core telemetry test.
Run `./run_real_stress.sh` to benchmark throughput on raw genomic data.
